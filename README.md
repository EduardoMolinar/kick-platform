# Claude Code Soccer Pack (DynamoDB-oriented)

This starter pack includes:

- `CLAUDE.md`
- `.mcp.json`
- `.claude/agents/*`
- `.claude/skills/*/SKILL.md`

## Notes

This variant is DynamoDB-oriented:
- Redis is used as the shared cache layer
- DynamoDB is referenced as the persistence layer for user profile and favorites
- No PostgreSQL example is included

Replace placeholder MCP URLs before use.
