# Backend

Placeholder for backend services — sports-api, profile-api, and an optional BFF.
Each service will live in its own subfolder with its own language, runtime, and deployment pipeline.

This folder is **not** part of the Nx workspace. Backend services run and deploy independently of the frontend.
